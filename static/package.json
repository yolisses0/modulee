{
	"author": "yolisses",
	"dependencies": {
		"modulee-engine-wasm": "^1.0.0"
	},
	"description": "The content that should be shared over network without the usual bundler due to WASM restrictions",
	"keywords": [],
	"license": "ISC",
	"main": "engine-processor.js",
	"name": "static",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"type": "commonjs",
	"version": "1.0.0"
}
